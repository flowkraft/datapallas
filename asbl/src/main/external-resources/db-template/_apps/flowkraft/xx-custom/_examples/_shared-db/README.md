# Shared directory: each billing-portal writes its SQLite DB file here
#   grails → grails-portal.db   ·   next → next-portal.db
# so the billing-portal-bkend can maintain them directly. Created empty on purpose.
